NPM environment variable set up

gitbash  open in amdin

On Linux or macOS:

export NODE_OPTIONS="--openssl-legacy-provider"
On Windows:

set NODE_OPTIONS="--openssl-legacy-provider"

sls deploy -f createAuction